---
import Base from "../layouts/Base.astro";
import { getSnapshot, coldStart } from "../lib/hyperliquid.ts";
import { pct, usd } from "../lib/funding.ts";
const snap = await getSnapshot((Astro.locals as any)?.runtime?.env?.SNAPSHOT, import.meta.env.DEV);
// Cold store: 503 + Retry-After rather than a page rendered from nothing.
if (!snap.available) return coldStart();
const rows = [...snap.perps].sort((a, b) => b.oiNotional - a.oiNotional);
const total = rows.reduce((s, p) => s + p.oiNotional, 0);
---
<Base title="Crypto perpetual open interest" description="Open interest and 24-hour volume for BTC, ETH, SOL and every other perpetual covered here, with turnover — volume as a multiple of open interest — which says how fast positioning is churning." fetchedAt={snap.fetchedAt}>
  <div class="page-head"><h1>Open interest</h1></div>
  <p style="margin:0 0 20px;max-width:74ch"><strong class="num">{usd(total)}</strong> across {rows.length} coins. The turnover column is 24h volume divided by open interest: a high number means the position base is churning rather than accumulating.</p>
  <div class="panel"><table class="tbl">
    <thead><tr><th>Coin</th><th class="num">Open interest</th><th class="num">24h volume</th><th class="num">Turnover (24h volume ÷ OI)</th><th class="num">Max leverage</th></tr></thead>
    <tbody>
      {rows.map((p) => (
        <tr>
          <td><a href={`/funding/${p.symbol.toLowerCase()}`}>{p.symbol}</a></td>
          <td class="num">{usd(p.oiNotional)}</td>
          <td class="num">{usd(p.dayNtlVlm)}</td>
          <td class="num">{p.oiNotional > 0 ? (p.dayNtlVlm / p.oiNotional).toFixed(2) + "×" : "—"}</td>
          <td class="num">{p.maxLeverage}×</td>
        </tr>
      ))}
    </tbody>
  </table></div>
  <style>
  </style>
</Base>
