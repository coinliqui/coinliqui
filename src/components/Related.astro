---
/**
 * A closing strip of real destinations, for the pages that are prose.
 *
 * The reference pages carried twelve internal links each — the navigation rail and the footer,
 * and nothing of their own. On a site whose whole argument is that its pages connect (a
 * contract to its liquidation map, a coin to its funding, a method to the page that applies
 * it), a prose page that links nowhere is a dead end for a reader and a leaf for a crawler.
 *
 * Every entry is a page that exists. There are no "coming soon" cards here, for the same
 * reason the rail renders unbuilt sections as spans: a link is a promise that something is on
 * the other side of it.
 */
interface Item { href: string; label: string; note: string }
const { items, title = "Where to go next" } = Astro.props as { items: Item[]; title?: string };
---

<h2 class="section">{title}</h2>
<div class="cards">
  {items.map((i) => (
    <a class="card" href={i.href}>
      <div class="card__label">{i.label}</div>
      {/* margin-top:8px OVERRIDES `.card__label + .card__value { margin-top: auto }` IN THE
          LAYOUT, and the override is the whole point. That rule bottom-anchors the value, which
          is right for a card whose value is one big number and wrong for one whose value is a
          sentence: with slack at the top, a note that runs to five lines starts higher than its
          three-line neighbours, and a row of four cards has three tops aligned and one that is
          not. Measured on /learn before the fix: cards 1–3 began their note 66.9px down, card 4
          began at 45.1px — a 21.8px step across a row of equal-height cards. The slack belongs
          at the BOTTOM, where nothing is reading. */}
      <div class="card__value" style="font-size:15px; line-height:1.45; height:auto; font-weight:500; margin-top:8px; margin-bottom:auto">{i.note}</div>
    </a>
  ))}
</div>
